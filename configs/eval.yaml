eval:
  checkpoint: lightning_logs/version_1/checkpoints/last.ckpt
  hparams_yaml: lightning_logs/version_1/hparams.yaml
  accelerator: gpu
  devices: [0]
  eval_one: false  
  segment_batch_size: 32

defaults:
  - data
  - _self_
